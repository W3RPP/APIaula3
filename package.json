{
  "name": "apinodejs",
  "version": "1.0.0",
  "description": "defeinir um local do computador para criar a pasta do projeto\r ```\r mkdir NOME_PROJETO\r ```\r acessar pasta do projeto\r ```\r cd NOME_PROJETO\r ```\r abrir a pasta VSCode\r ```\r code .\r ```\r iniciar gerenciardor de pacotes node\r ```\r npm init -y\r ```\r criar o arquivo .gitignore\r ```\r touch .gitignore\r ```\r criar o arquivo .env\r ```\r touch .env\r ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  }
}
